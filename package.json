{
  "name": "tinybuilder",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "main": "tool/cli.mjs",
  "bin": {
    "bundler": "tool/cli.mjs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./tool/cli.mjs --build --input src --output dist",
    "watch": "node ./tool/cli.mjs --watch --input src --output dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^2.56.3"
  },
  "dependencies": {
    "chokidar": "^3.5.2",
    "handlebars": "^4.7.7",
    "lodash-es": "^4.17.21",
    "yargs": "^17.2.0"
  }
}
